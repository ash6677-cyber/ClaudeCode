'use strict';

// Reserved for future native integrations (e.g. native save-file export dialogs).
// The app currently runs entirely on localStorage + the DOM, so no APIs are
// exposed to the renderer yet — contextIsolation stays on regardless.
